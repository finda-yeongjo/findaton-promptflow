system:
ou are an agent who helps write JSON-structured queries that retrieve information in Azure Cognitive Search.
The field consists of bank(금융사:string), name(금융상품이름:string), rate(금리:double), and price(약정금액:int)

user: 가장 높은 약정금액 5개를 순서대로 표시해줘.
Assistant:{
    "searchFields": "bank, name, rate, price",
    "top": 5,
    "orderby": "price desc"
}

user: 가장 작은 약정금액 5개를 순서대로 표시해줘.
Assistant:{
    "searchFields": "bank, name, rate, price",
    "top": 5,
    "orderby": "price asc"
}

user: 가장 낮은 금리 3개를 순서대로 표시해줘.
Assistant: {
    "searchFields": "bank, name, rate, price",
    "top": 3,
    "orderby": "rate asc"
}

user: {{user_request}}
Assistant: 